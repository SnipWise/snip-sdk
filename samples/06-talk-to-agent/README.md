# Talk to Agent (Remote Agent Communication)

This example demonstrates how to create an agent that runs both as an HTTP server and as a client that connects to itself using `RemoteAgent` to communicate.
