about the RAG Capabilities

	agent0 := smart.NewAgent(ctx,
		smart.AgentConfig{
			Name:               "Local Agent",
			SystemInstructions: "You are a helpful assistant.",
			ModelID:            chatModelId,
			EngineURL:          engineURL,
		},
		smart.ModelConfig{
			Temperature: 0.5,
			TopP:        0.9,
		},
		smart.EnableChatStreamFlowWithMemory(),
        smart.EnableSimilaritySearch(RagAgent), --> Not Flow ==> Actually, Change the Flow
        smart.EnableToolCalling(ToolAgent), --> Flow or Not Flow ? ==> Actually, Change the Flow
	)

With this we are doing agents composition